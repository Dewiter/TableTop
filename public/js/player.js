class Player {
    constructor (config) {
        this.hp     = config.hp;
        this.pm     = config.pm;
        this.weapon = config.weapon; 
    }

    spawnPlayer () {

    }
}

export default Player;